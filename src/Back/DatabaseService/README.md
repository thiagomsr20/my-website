# Responsabilidade desse projeto

Armazenar contatos que foram inseridos no formulário, como Email nome e mensagem,
dados importantes para futuros projetos

API terá dois métodos, GET e POST

GET não será usado no portfólio de primeiro momento, apenas o POST, para realizar inserção dos dados